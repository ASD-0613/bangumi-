# Bangumi 番剧数据查询工具（bangumi_query）v3.0.0

基于 **Bangumi API** 的番剧数据查询工具，以 **Windows 单文件 exe** 形式分发
（PyQt5 图形界面，双击即用，无需安装 Python）。

功能：

1. **搜索番剧** —— 关键词搜索，表格展示，分页浏览；
2. **番剧详情** —— 封面、基本信息、状态、地区、评分、简介、主要声优、
   制作团队与分集列表；
3. **排行榜** —— 类别 × 排序维度双重筛选，50 条数据、每页 10 条可翻页；
4. **追番日历** —— 星期按钮一次只查看一天的更新内容，平铺显示每部番剧；
5. **本地缓存** —— 封面图片磁盘缓存，跨标签页 / 跨启动复用，
   主窗口底部可查看占用并一键清除；
6. **已看完** —— 详情页标题右侧一键打钩标记（本机保存）；
   Steam 仓库式网格陈列封面 + 简中名，单击选中、双击进入详情。
   搜索/排行榜/追番日历三处数据栏**行高约 3 倍、字号放大**，并都有独立
   的**封面首列**。

## 安装与运行

**普通用户**：到 [Releases](https://github.com/ASD-0613/bangumi-/releases)
下载 `BangumiQuery-<版本号>.zip`，解压后双击其中的 exe 即可（无控制台窗口、
无需安装 Python）。首次运行如出现 SmartScreen 警告，点「更多信息」→
「仍要运行」（未签名 exe 的正常现象）。

**开发者**（源码仅用于调试与打包，需 Python 3.10+，能访问 `api.bgm.tv`）：

```bash
pip install -r requirements.txt        # PyQt5、requests
python -m bangumi_query.main        # 开发调试运行 GUI
pip install pyinstaller && python build_exe.py   # 打包 exe
```

### GUI 用法要点

- **排版统一**：搜索结果/排行榜/追番日历三处数据栏：每行高度约为默认的
  **3 倍**（84px），数据字号放大至 12pt，并都带独立的**“封面”首列**；
  **封面图的纵向高度与行高一致**（等比缩放保留比例），并在单元格内
  **居中**显示（异步加载、带磁盘缓存，见“本地缓存”）；
- **表格列宽可拖拽**：搜索/排行榜表头可像 Excel 一样拖动分隔线调整宽度；
- **排行榜**：“榜单类别”（全部/日漫/国漫/纪录片/电影）＋“排序维度”
  （热度 / 评分 / 收藏数），**任一下拉改动立即刷新**；首次切到排行榜
  标签页时自动加载一次；请求进行中改动筛选不会丢失（结果返回后自动
  按最新选择补发）；每页 10 条可翻页；双击或回车查看详情；
- **追番日历**：“周一 ~ 周日”单选按钮（默认今天）只展示所选当天的更新；
  “日历类型”下拉改动立即刷新，首次切到该标签页时自动加载一次；无数据
  或当日无更新时显示灰色占位行；数据区**没有“周一/周二”可折叠分组行**，
  条目直接平铺；列结构为“**封面 | 番剧名称 | 开播时间**”，名称列约占
  数据栏一半宽（窗口缩放自适应）；“开播时间”= 首话播出日期，逐条异步
  查询并填充（查询中显示 …，失败显示 —）；双击或回车番剧查看详情；
- **详情页**：从搜索结果/榜单/日历均可进入（双击、回车或按钮）；加载中
  改选其他条目立即生效（同一条目加载中重复点击只发一次请求）；分集
  超过 80 条时显示“仅展示前 N 集”截断提示；
  - **已看完打钩**：标题行最右侧的粗黑边圆角方框——未选中为白底浅灰钩，
    点击后变绿底纯黑钩并稍微放大；点击即时保存到本机 `watched.json`
    （与封面缓存同级，不受“清除缓存”影响）；
- **已看完**：追番日历右侧的页签，Steam 仓库式网格陈列已打钩的番剧
  （封面在上、简中名在下），单击选中、双击进入详情；封面复用磁盘缓存，
  最新打钩的排最前；
- **分页按钮**：搜索/排行榜的“上一页 / 下一页”按当前页码自动禁用
  （首页禁用上一页、末页禁用下一页）；
- **错误恢复**：请求失败弹窗提示后，按钮与下拉自动恢复可用，可直接重试
  （不会卡在禁用状态）；
- **底部缓存栏**：显示本地缓存的文件数与占用，右下角“清除缓存”点击后
  弹窗三选：**一并清除**（缓存 + 「已看完」记录）/ **仅清缓存**（默认，
  保留记录）/ **取消**；关闭弹窗（X / Esc）等同取消，什么都不清；
- **全局字体**：应用整体使用微软雅黑；页签名、搜索框、“已看完”番剧名
  等显眼位置为加粗字体。

## 数据来源与接口

接口请求统一携带 `User-Agent: BangumiQuery/<版本号>`
（Bangumi 要求 UA，缺失时返回 403）；封面图片由独立的下载线程获取
（使用浏览器 UA），并写入本地磁盘缓存。

| 功能 | 接口（真实验证） | 说明 |
| --- | --- | --- |
| 搜索番剧 | `POST https://api.bgm.tv/v0/search/subjects` | 关键词 + `filter.type` |
| 番剧详情 | `GET https://api.bgm.tv/v0/subjects/{id}` | 基本信息/评分/简介 |
| 分集列表 | `GET https://api.bgm.tv/v0/episodes?subject_id={id}` | 仅正片（过滤 SP/OP/ED“第0话”等） |
| 角色声优 | `GET https://api.bgm.tv/v0/subjects/{id}/characters` | 动画详情的“主要声优” |
| 排行榜 | `GET https://api.bgm.tv/v0/subjects?type=&sort=rank&limit=&offset=` | 可翻页候选池 → 本地按维度排序取 50 |
| 追番日历 | `GET https://api.bgm.tv/calendar` | 根路径（不带 /v0），`weekday(1..7)` + `items` |

注意：`POST /v0/search/subjects` 的空关键词榜单每请求固定返回前 10 且分页
无效；榜单因此改用 `GET /v0/subjects` 的候选池实现（POST 的 `sort=heat` 仅
用于普通带关键词搜索等场景）。

## 字段映射（按 Bangumi 真实返回）

| Bangumi 字段 | 映射 |
| --- | --- |
| `name_cn`（无则 `name`） | 标题 |
| `summary` | 简介（空 → “暂无简介”） |
| `rating.score` | 评分（0-10，1 位小数） |
| `date` / `air_date` | 播出时间（YYYY-MM-DD） |
| `/v0/episodes` 的 `data[]`（type=0） | 分集列表 |
| `eps`（数字） | 总集数 |
| `collection.*` / `collects` | 收藏数（排行榜“收藏数”维度、详情收藏人数） |
| `tags` / `meta_tags` | 地区（日本/中国/美国/欧美…） |
| characters 的 `actors[]` | 主要声优（角色 → 声优） |
| infobox“放送结束/播放结束/播出结束” | 状态=已完结 |

说明：
- **地区**：Bangumi 详情无 infobox“地区”键，地区取自 `tags`/`meta_tags`
  词表（如“日本/中国/欧美”）并去重；
- **状态**：能确认完结才显示“已完结”；开播日期在约 210 天内且无完结信息
  时保守显示“连载中”；其余显示 “—”；
- **分集**：`/v0/episodes` 会返回“第0话/SP”等非正片条目，程序只保留
  `type=0`（或未标注）的正片，避免多出多余集数。

## 排行榜说明

- 数据流：从 `GET /v0/subjects?type=&sort=rank` 抓取候选池（默认 150 条，
  每页 25、真正 offset 翻页）→ “日漫/国漫”按条目标签地区过滤 → 按
  **排序维度**本地降序 → 取前 50 条；
- 排序维度含义：**热度**=评分人数、**评分**=rating.score、**收藏数**=
  collection 各状态总数；
- `纪录片` / `电影` 使用 type=6（三次元真人影视）候选池，Bangumi 未细分
  纪录片/电影，作为近似数据；
- 若某类别候选池过滤后无匹配，会给出明确提示，不会混入他国条目。

## 网络与代理配置

| 环境变量 | 默认 | 说明 |
| --- | --- | --- |
| `BANGUMI_PROXY` | 未设置 | HTTP(S) 代理，如 `http://127.0.0.1:7890` |
| `BANGUMI_API_BASE` | `https://api.bgm.tv/v0` | 自设网关/镜像（需含 `/v0`） |
| `BANGUMI_CALENDAR_URL` | `https://api.bgm.tv/calendar` | 日历接口地址 |
| `BANGUMI_CONNECT_TIMEOUT` / `BANGUMI_READ_TIMEOUT` | `5` / `10` 秒 | 连接/读取超时 |
| `BANGUMI_RETRY_TIMES` | `2` | 单次请求总尝试次数（含首次；默认 2 即超时/429/5xx 后重试 1 次） |
| `BANGUMI_INSECURE` | `0` | `1` 时关闭 SSL 校验（不推荐） |
| `BANGUMI_CACHE_DIR` | 平台默认 | 封面图片磁盘缓存目录 |

未设置代理时信任系统代理（Windows 注册表代理 / `HTTP_PROXY` /
`HTTPS_PROXY`）；仅 DEBUG 或首次请求打印 `[代理] 使用代理: …`。
网络排障：`python check_network.py`。

## 本地缓存

- 封面图片首次下载成功后写入本地缓存目录（默认 Windows 为
  `%LOCALAPPDATA%\BangumiQuery\cache`，其它平台 `~/.cache/BangumiQuery`，
  可用 `BANGUMI_CACHE_DIR` 覆盖）；缓存按封面图片 URL 区分，同一封面在
  搜索/排行榜/追番日历/详情之间以及下次启动时**直接读缓存，不再重复下载**；
- 主窗口底部显示缓存占用（文件数与大小），右下角“清除缓存”按钮可一键
  清空，成功后弹窗提示；
- 接口 JSON 数据不缓存，保证评分 / 榜单等数据的时效性。

## 目录结构

```
项目根目录/
├── build_exe.py              # Windows 单文件 exe 打包脚本（PyInstaller）
├── make_release.py           # 打 zip + 创建/覆盖 GitHub Release（一键发版）
├── check_network.py          # 网络诊断工具（开发者脚本）
├── requirements.txt
├── README.md
├── bangumi_query/
│   ├── config.py             # 配置（应用名/版本号唯一来源、代理/超时、榜单参数）
│   ├── main.py               # GUI（PyQt5）
│   ├── api/bangumi.py        # 数据层：Bangumi API v0 封装
│   ├── models/bangumi.py     # 数据模型 + 字段映射 + 状态/地区推断
│   └── utils/
│       ├── cache.py          # 本地磁盘缓存（封面图片，BANGUMI_CACHE_DIR 可覆盖）
│       └── watched.py        # “已看完”标记持久化（watched.json）
└── tests/                    # 离线测试（解析/缓存/已看完/GUI 冒烟）
```

## 运行测试

```bash
python -m unittest discover -s tests -v
```

GUI 测试以 `QT_QPA_PLATFORM=offscreen` 离屏运行；无 PyQt5 时自动跳过。

## 打包为 exe（Windows）

生成项目的分发形态——无需安装 Python 的单文件可执行程序（无控制台黑窗）：

```bash
pip install pyinstaller   # 仅构建机需要，运行 exe 的用户不需要
python build_exe.py       # 产物：dist/BangumiQuery-<版本号>.exe
```

- 版本号自动取自 `config.VERSION`；构建参数（onefile + windowed 及依赖排除
  清单）集中在 `build_exe.py`；
- 首次运行如出现 SmartScreen 警告：点「更多信息」→「仍要运行」（未签名
  exe 的正常现象）；
- exe 的封面缓存与脚本版一致（`%LOCALAPPDATA%\BangumiQuery\cache`），
  与 exe 所在位置无关；
- 构建脚本已排除 Anaconda 环境预装的 PySide6 / matplotlib / numpy 等无关
  大件（PyInstaller 不允许两套 Qt 绑定并存）；若你的环境不同导致构建失败，
  按报错信息增删 `build_exe.py` 中的 `EXCLUDES` 即可。

## 修订记录

- **v3.0.0**
  - 新增**「已看完」页签**（追番日历右侧）：Steam 仓库式网格陈列已打钩的
    番剧——封面在上、简中名在下，单击选中、双击进入详情；封面复用磁盘
    缓存，最新打钩的排最前；
  - 详情页标题行最右侧新增**打钩框**（粗黑边圆角方框）：未选中为白底
    浅灰钩，点击后变绿底纯黑钩并稍微放大；标记即时持久化到本机
    `watched.json`（位于缓存目录上一级，“清除缓存”不影响）；
  - 新增 `make_release.py`：一条命令完成 zip 打包 + GitHub Release
    创建/覆盖（发版策略：一个分支最多一个 Release，重发即覆盖）；
  - **界面美化**：全局字体切换为微软雅黑（修复 Windows 默认字体渲染
    中文发虚的问题）；页签名、搜索框、“已看完”番剧名等显眼文字加粗
    放大；搜索框纵向加高（36px）；
  - “清除缓存”改为先弹窗三选：**一并清除 / 仅清缓存（默认）/ 取消**，
    是否连带清除「已看完」记录由用户决定；关闭弹窗等同取消，支持反悔；
  - **修复关闭程序时偶发卡死**：此前对卡在网络请求上的后台线程执行
    terminate() 有死锁风险；现改为窗口立即隐藏 + 数据即时落盘 +
    必要时直接结束进程，退出永远干脆；
  - 新增程序图标（exe 文件图标与运行时窗口/任务栏图标）；
- **v2.9.0**
  - 分发方式变更：**移除两个非 exe 打开方式**——`python run.py` 启动脚本与
    命令行版（`cli_main.py` / `utils/display.py`，rich 依赖随之移除），
    exe 成为唯一使用入口；
  - `build_exe.py` 改用构建时生成的临时入口（不再依赖 run.py），并继续
    排除 Anaconda 环境中的 PySide6 / matplotlib 等无关大件；
  - Release 附件 zip 内含 exe，解压即可使用；
  - 数据层、GUI 功能与 v2.8.0 完全一致，仅分发形态变化。
- **v2.8.0**
  - 新增 Windows 单文件打包：`python build_exe.py` 一键生成无控制台的
    `BangumiQuery-<版本号>.exe`（详见"打包为 exe"一节）；
  - 封面图片**本地磁盘缓存**：首次下载成功后写入缓存目录（环境变量
    `BANGUMI_CACHE_DIR` 可自定义，默认 `%LOCALAPPDATA%\BangumiQuery\cache`
    或 `~/.cache/BangumiQuery`），再次显示同一封面（搜索/排行榜/日历/详情
    之间、以及重启后）直接读缓存，不再重复下载；主窗口底部新增缓存占用
    提示与**“清除缓存”按钮**（成功后弹窗提示）；
  - 修复**请求失败后界面卡死**：搜索/排行榜/追番日历请求失败时按钮自动
    恢复可用（此前一次网络错误会使对应功能永久禁用，只能重启程序）；
  - 排行榜：“榜单类别”改动现与“排序维度”一致**立即刷新**；请求进行中的
    筛选改动不再丢失（结果返回后自动按最新选择补发）；首次切到排行榜
    标签页自动加载一次；
  - 追番日历：“日历类型”改动立即刷新；首次切到该标签页自动加载一次；
    空数据/当日无更新显示灰色占位行；“开播时间”查询失败时正常落到
    “—”（此前可能永久停留在 “…”）；
  - 详情：加载中改选其他条目立即生效（token 打断旧请求，不再“点了没
    反应”）；同一条目加载中重复点击只发一次请求；分集超过 80 条时显示
    截断提示；
  - 搜索/排行榜分页按钮按页码自动禁用；表格/日历支持**回车键**打开详情；
  - 退出程序时后台线程获得约 2 秒收尾窗口，降低退出崩溃概率。
- **v2.7.3**
  - 修复封面“视觉上依然很小”的真正原因：Qt 表格/树形控件默认以小型
    iconSize 绘制单元格图标，现在为搜索/排行榜/追番日历统一设置
    `setIconSize(84×84)`，封面按行高（84px）实际显示并居中。
- **v2.7.2**
  - 修复封面“仍然很小”的问题：真正将搜索/排行榜表格行高**锁定为固定
    84px**（此前排行榜残留 48px 覆盖、表格行高未被固定）；追番日历行高
    同样为 84px；
  - 封面显示高度=行高 84px（等比缩放）且居中生效；封面列宽相应加宽。
- **v2.7.1**
  - 三处数据栏（搜索/排行榜/追番日历）的封面图：**纵向高度与行高一致**
    （84px，等比缩放保留比例），并统一在封面单元格内**居中**显示。
- **v2.7**
  - 追番日历：移除“周一/周二”等可折叠分组行（条目直接平铺）；移除
    “更新集数”列；列结构改为“封面 | 番剧名称 | 开播时间”，“开播时间”
    = 首话播出日期（逐条查询分集接口异步填充）；
  - 搜索结果新增独立“封面”首列（与排行榜/日历一致，异步加载+缓存）；
  - 搜索结果/排行榜/追番日历三处：行高约放大至默认 3 倍（84px）、字号
    放大（12pt）、封面随行高自适应；
  - 版本号 2.7.0。
- **v2.6**
  - 排行榜：封面与名称分离为独立“封面”首列，图标尺寸随数据行高
    自适应（居中显示）；
  - 追番日历：
    - 新增“周一~周日”单选行（默认今天），一次只查看一天的更新内容；
    - 列结构改为“封面 | 番剧名称 | 更新时间 | 更新集数”，番剧名称列
      约占数据栏一半宽（窗口缩放自适应）；
    - “更新内容”列改为“**更新集数**”，实时查询分集接口显示“第 N 话”
      （每行一次请求，后台线程执行、带缓存）；
    - 每条数据在封面列加载封面缩略图（异步+缓存）。
  - 数据层新增 `fetch_airing_episode_number`（按放送日期解析话数）；
  - 版本号 2.6.0。
- **v2.5**
  - 搜索结果/排行榜表格列宽可拖拽（表头 Interactive）；
  - 排行榜改名并重做：类别 × 排序维度（热度/评分/收藏数）双重筛选，
    可翻页取 50 条（GET /v0/subjects 候选池 + 本地排序）；
  - 搜索/详情状态推断改进：可确认完结 → “已完结”，近期开播无完结信息 →
    “连载中”，其余 “—”（不再清一色 “—”或臆测连载中）；
  - 分集只保留正片（过滤“第0话/SP”等 type=1 条目），修复多余集数；
  - 应用名统一为 “Bangumi 番剧数据查询工具”、版本 2.5.0（单一来源
    `config.APP_NAME/VERSION`，改名只改该处并同步 README）。
- **v2.3**：排行榜改用热度语义；详情补充主要声优（characters 接口）；
  状态不再臆测“连载中”。
- **v2.2**：分集改用 `/v0/episodes?subject_id=`；地区改从 tags/meta_tags 提取；
  “日漫/国漫”榜单按地区标签过滤。
- **v2.1**：修复 GUI 后台线程未启动；日历改用 `https://api.bgm.tv/calendar`；
  网络加固（超时/重试/代理/诊断脚本）。

## 已知限制

- “热度”维度以评分人数为代理；评分维度以均分为准（经典高分作品靠前，
  属预期语义）；
- 纪录片/电影仅为 type=6 近似数据；
- Bangumi 不提供播放量等字段，相关列显示 “—”；
- 播出状态无法 100% 可靠（Bangumi 无权威状态字段），程序采用保守推断；
- 追番日历仅覆盖正在放送的动画；“日历类型”（番剧/国创/影视）依赖
  Bangumi 日历数据，暂无法区分日漫/国漫/影视，三个选项当前返回相同内容；
- 追番日历“开播时间（首话日期）”为逐条异步查询（带缓存），首次查看某一天
  会稍慢，失败条目显示 “—”；
- 封面图来自第三方图片服务器，加载失败显示占位；封面下载未使用
  `BANGUMI_PROXY` 指定的代理（系统代理 / `HTTP_PROXY` 等仍然生效）。
